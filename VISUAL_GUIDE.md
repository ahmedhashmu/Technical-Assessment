# Visual Guide - TruthOS Meeting Intelligence

## 🎨 System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              Next.js Frontend (React)                     │  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐         │  │
│  │  │ Home Page  │  │ Contacts   │  │ Submit     │         │  │
│  │  │            │  │ Dashboard  │  │ Meeting    │         │  │
│  │  └────────────┘  └────────────┘  └────────────┘         │  │
│  │                                                            │  │
│  │  Components:                                              │  │
│  │  • MeetingCard • MeetingForm • Navbar • Footer           │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↓ HTTP Requests
┌─────────────────────────────────────────────────────────────────┐
│                    Next.js API Routes (Proxy)                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  /api/meetings              → POST new meeting           │  │
│  │  /api/meetings/[id]/analyze → POST analyze meeting       │  │
│  │  /api/contacts/[id]/meetings → GET contact meetings      │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↓ Forwards to
┌─────────────────────────────────────────────────────────────────┐
│                    FastAPI Backend (Python)                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                        Routers                            │  │
│  │  • meetings.py  • analysis.py  • contacts.py             │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              ↓                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                       Services                            │  │
│  │  • MeetingIngestionService                               │  │
│  │  • AnalysisEngine                                        │  │
│  │  • LLMClient (OpenAI)                                    │  │
│  │  • QueryService                                          │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              ↓                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    Database Models                        │  │
│  │  • Meeting  • MeetingAnalysis  • Contact                 │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↓ SQL Queries
┌─────────────────────────────────────────────────────────────────┐
│                  PostgreSQL Database (Neon)                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Table: meetings (IMMUTABLE)                             │  │
│  │  ├─ id, contact_id, type, occurred_at                    │  │
│  │  ├─ transcript, created_at                               │  │
│  │  └─ 🔒 Triggers: Prevent UPDATE/DELETE                   │  │
│  │                                                            │  │
│  │  Table: meeting_analyses (DERIVED)                       │  │
│  │  ├─ id, meeting_id (FK)                                  │  │
│  │  ├─ sentiment, topics, objections                        │  │
│  │  ├─ commitments, outcome, summary                        │  │
│  │  └─ analyzed_at                                          │  │
│  │                                                            │  │
│  │  Table: contacts                                         │  │
│  │  └─ id, name, created_at                                 │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘

External Services:
┌─────────────────────┐
│   OpenAI API        │
│   (GPT-4o-mini)     │ ← Called by LLMClient for analysis
└─────────────────────┘
```

---

## 📊 Data Flow Diagrams

### Flow 1: Submit New Meeting

```
User fills form
    ↓
┌─────────────────────────────────────────────────────────┐
│ Frontend: MeetingSubmissionForm.tsx                     │
│ • Collects: meetingId, contactId, type, date, transcript│
│ • Converts datetime to ISO format                       │
│ • Calls: apiClient.createMeeting(data)                  │
└─────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ API Route: /api/meetings/route.ts                       │
│ • Receives request from frontend                        │
│ • Forwards to: http://localhost:8000/api/meetings       │
│ • Returns response to frontend                          │
└─────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ Backend Router: meetings.py                             │
│ • Validates data with Pydantic (MeetingCreate schema)   │
│ • Calls: MeetingIngestionService.create_meeting()       │
│ • Returns: MeetingResponse                              │
└─────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ Service: MeetingIngestionService                        │
│ • Creates Meeting object                                │
│ • Saves to database: db.add(meeting)                    │
│ • Commits transaction: db.commit()                      │
│ • Returns meeting                                       │
└─────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ Database: PostgreSQL                                    │
│ • INSERT INTO meetings VALUES (...)                     │
│ • Triggers check: No UPDATE/DELETE allowed              │
│ • Returns: Created meeting record                       │
└─────────────────────────────────────────────────────────┘
    ↓
Success! Meeting saved immutably ✅
```

---

### Flow 2: Analyze Meeting

```
User clicks "Analyze" button
    ↓
┌─────────────────────────────────────────────────────────┐
│ Frontend: MeetingCard.tsx                               │
│ • Calls: apiClient.analyzeMeeting(meetingId)            │
│ • Shows loading spinner                                 │
└─────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ API Route: /api/meetings/[id]/analyze/route.ts          │
│ • Forwards to backend                                   │
└─────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ Backend Router: analysis.py                             │
│ • Calls: AnalysisEngine.analyze_meeting(meeting_id)     │
└─────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ Service: AnalysisEngine                                 │
│ 1. Get meeting: ingestion_service.get_meeting(id)       │
│ 2. Extract signals: llm_client.extract_signals(text)    │
│ 3. Create analysis: MeetingAnalysis(...)                │
│ 4. Save: db.add(analysis), db.commit()                  │
└─────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ Service: LLMClient                                      │
│ • Build structured prompt with JSON schema              │
│ • Call OpenAI API (3 retries with backoff)              │
│ • Parse JSON response                                   │
│ • Validate with Pydantic (AnalysisSignals)              │
│ • If fails: Use demo mode (keyword analysis)            │
└─────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ OpenAI API (or Demo Mode)                               │
│ Returns:                                                │
│ {                                                       │
│   "sentiment": "positive",                              │
│   "topics": ["pricing", "timeline"],                    │
│   "objections": ["concerns about cost"],                │
│   "commitments": ["follow up next week"],               │
│   "outcome": "follow_up",                               │
│   "summary": "Positive meeting..."                      │
│ }                                                       │
└─────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ Database: PostgreSQL                                    │
│ • INSERT INTO meeting_analyses VALUES (...)             │
│ • Links to meeting via meeting_id (foreign key)         │
└─────────────────────────────────────────────────────────┘
    ↓
Analysis complete! Insights displayed ✅
```

---

### Flow 3: View Contact Meetings

```
User navigates to /contacts/contact_001
    ↓
┌─────────────────────────────────────────────────────────┐
│ Frontend: contacts/[id]/page.tsx                        │
│ • useEffect: fetchMeetings(contactId)                   │
│ • Calls: apiClient.getContactMeetings(contactId)        │
└─────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ API Route: /api/contacts/[id]/meetings/route.ts         │
│ • Forwards to backend                                   │
└─────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ Backend Router: contacts.py                             │
│ • Calls: QueryService.get_contact_meetings(contact_id)  │
└─────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ Service: QueryService                                   │
│ 1. Query meetings:                                      │
│    SELECT * FROM meetings                               │
│    WHERE contact_id = 'contact_001'                     │
│    ORDER BY occurred_at DESC                            │
│                                                         │
│ 2. For each meeting, get latest analysis:              │
│    SELECT * FROM meeting_analyses                       │
│    WHERE meeting_id = meeting.id                        │
│    ORDER BY analyzed_at DESC LIMIT 1                    │
│                                                         │
│ 3. Combine into MeetingWithAnalysis objects             │
└─────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ Returns:                                                │
│ {                                                       │
│   "contactId": "contact_001",                           │
│   "meetings": [                                         │
│     {                                                   │
│       "id": "meeting_002",                              │
│       "transcript": "...",                              │
│       "analysis": {                                     │
│         "sentiment": "positive",                        │
│         "topics": [...],                                │
│         ...                                             │
│       }                                                 │
│     },                                                  │
│     { ... more meetings ... }                           │
│   ]                                                     │
│ }                                                       │
└─────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ Frontend: Renders MeetingCard for each meeting          │
│ • Shows meeting details                                 │
│ • Shows analysis if available                           │
│ • "Analyze" button if not analyzed                      │
└─────────────────────────────────────────────────────────┘
    ↓
User sees complete conversation history! ✅
```

---

## 🔐 Security & Data Integrity

### Immutability Enforcement

```
┌─────────────────────────────────────────────────────────┐
│ Someone tries to modify a meeting:                      │
│ UPDATE meetings SET transcript = 'HACKED'               │
│ WHERE id = 'meeting_001'                                │
└─────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ PostgreSQL Trigger: prevent_meeting_update_trigger      │
│ • Intercepts UPDATE command                             │
│ • Executes: prevent_meeting_update() function           │
│ • RAISES EXCEPTION: "Meetings are immutable"            │
│ • Transaction ROLLED BACK                               │
└─────────────────────────────────────────────────────────┘
    ↓
❌ UPDATE BLOCKED! Data integrity preserved ✅
```

### API Key Security

```
❌ BAD: Frontend calls OpenAI directly
┌──────────────┐
│   Browser    │ → OpenAI API (API key exposed!)
└──────────────┘

✅ GOOD: Next.js API routes proxy
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   Browser    │ →   │  Next.js API │ →   │  FastAPI     │ → OpenAI
└──────────────┘     │  (Server)    │     │  (Server)    │
                     │  API key safe│     │  API key safe│
                     └──────────────┘     └──────────────┘
```

---

## 🎯 Component Relationships

### Frontend Component Tree

```
App (layout.tsx)
├── ThemeProvider (Material-UI)
│   ├── Navbar
│   │   ├── Logo
│   │   ├── Navigation Links
│   │   └── Buttons
│   │
│   ├── Page Content (children)
│   │   │
│   │   ├── Home Page (page.tsx)
│   │   │   ├── Hero Section
│   │   │   ├── Feature Cards
│   │   │   └── Call to Action
│   │   │
│   │   ├── Contacts Page (contacts/page.tsx)
│   │   │   ├── Search Box
│   │   │   ├── Sample Contact Cards
│   │   │   └── Instructions
│   │   │
│   │   ├── Contact Detail (contacts/[id]/page.tsx)
│   │   │   ├── Contact Header
│   │   │   └── MeetingCard (for each meeting)
│   │   │       ├── Meeting Info
│   │   │       ├── Transcript Preview
│   │   │       ├── Analysis Display
│   │   │       └── Expandable Details
│   │   │
│   │   └── New Meeting (meetings/new/page.tsx)
│   │       └── MeetingSubmissionForm
│   │           ├── Meeting ID Input
│   │           ├── Contact ID Input
│   │           ├── Type Selector
│   │           ├── DateTime Picker
│   │           ├── Transcript TextArea
│   │           └── Submit Button
│   │
│   └── Footer
│       ├── Links
│       └── Social Icons
│
└── API Client (lib/api-client.ts)
    ├── createMeeting()
    ├── analyzeMeeting()
    └── getContactMeetings()
```

---

## 📦 File Organization Logic

### Why This Structure?

```
backend/
├── app/
│   ├── main.py              ← Entry point (start here)
│   ├── core/                ← Configuration (settings)
│   ├── db/                  ← Database setup
│   ├── models/              ← Data structures (tables)
│   ├── routers/             ← API endpoints (routes)
│   └── services/            ← Business logic (the brain)
```

**Separation of Concerns:**
- `routers/` = "What endpoints exist?"
- `services/` = "What do those endpoints do?"
- `models/` = "What does the data look like?"
- `db/` = "How do we connect to database?"
- `core/` = "What are our settings?"

```
frontend/
├── app/
│   ├── layout.tsx           ← Wraps everything
│   ├── page.tsx             ← Home page
│   ├── api/                 ← Backend proxy (server-side)
│   ├── contacts/            ← Contact pages
│   └── meetings/            ← Meeting pages
├── components/              ← Reusable UI pieces
├── lib/                     ← Utilities (API client, theme)
└── types/                   ← TypeScript definitions
```

**Why separate components?**
- Reusable across pages
- Easier to test
- Consistent UI

---

## 🚀 Deployment Architecture

```
┌─────────────────────────────────────────────────────────┐
│                    Vercel (Frontend)                     │
│  • Hosts Next.js app                                    │
│  • Serves static files from CDN                         │
│  • Runs API routes as serverless functions              │
│  • Auto-scales based on traffic                         │
└─────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────┐
│              Backend Server (FastAPI)                    │
│  • Can be deployed to:                                  │
│    - Heroku                                             │
│    - AWS Lambda                                         │
│    - Google Cloud Run                                   │
│    - DigitalOcean                                       │
│  • Handles business logic                               │
│  • Connects to database                                 │
└─────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────┐
│              Neon PostgreSQL (Database)                  │
│  • Serverless PostgreSQL                                │
│  • Auto-scaling                                         │
│  • Connection pooling                                   │
│  • Automatic backups                                    │
└─────────────────────────────────────────────────────────┘
```

---

## 💡 Key Insights

### 1. Why Proxy Layer (Next.js API Routes)?

**Without Proxy:**
```
Browser → Backend (CORS issues, exposed secrets)
```

**With Proxy:**
```
Browser → Next.js API → Backend (secure, no CORS)
```

### 2. Why Separate Tables?

**meetings** = Source of truth (immutable)
**meeting_analyses** = Derived insights (can regenerate)

If we improve our AI model, we can:
- Re-analyze all meetings
- Keep original transcripts unchanged
- Compare old vs new analysis

### 3. Why TypeScript?

```typescript
// JavaScript (no types)
function createMeeting(data) {
  // What is data? Who knows! 🤷
}

// TypeScript (with types)
function createMeeting(data: MeetingFormData) {
  // data must have: meetingId, contactId, type, etc.
  // Editor shows autocomplete
  // Catches bugs before running
}
```

### 4. Why Material-UI?

Instead of building from scratch:
```tsx
// Without MUI (lots of work)
<div className="card">
  <div className="card-header">...</div>
  <div className="card-body">...</div>
</div>

// With MUI (ready to use)
<Card>
  <CardHeader>...</CardHeader>
  <CardContent>...</CardContent>
</Card>
```

---

## 🎓 Learning Path

If you want to understand more:

1. **Start with Frontend:**
   - Look at `app/page.tsx` (simplest page)
   - Then `components/MeetingCard.tsx` (reusable component)
   - Then `app/contacts/[id]/page.tsx` (data fetching)

2. **Move to API Routes:**
   - Look at `app/api/meetings/route.ts` (simple proxy)
   - Understand how it forwards requests

3. **Explore Backend:**
   - Start with `routers/meetings.py` (API endpoint)
   - Then `services/ingestion.py` (business logic)
   - Then `models/meeting.py` (database structure)

4. **Understand Database:**
   - Look at `db/init_db.py` (setup)
   - See how triggers work

5. **Study AI Integration:**
   - Look at `services/llm_client.py` (OpenAI calls)
   - Understand bounded agent design

---

This is a professional, production-ready system! 🎉
