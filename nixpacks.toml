[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = [
  "chmod +x build.sh",
  "./build.sh"
]

[phases.start]
cmd = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
